/** Split out from `employee-grpc-client.module.ts` to avoid a circular import - same reasoning as every other service's own `*-grpc-client.constants.ts`. */
export const EMPLOYEE_GRPC_PACKAGE = 'EMPLOYEE_GRPC_PACKAGE';
