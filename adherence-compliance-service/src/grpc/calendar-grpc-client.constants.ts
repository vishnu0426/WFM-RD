/** Split out from `calendar-grpc-client.module.ts` to avoid a circular import - same reasoning as every other service's own `*-grpc-client.constants.ts`. */
export const CALENDAR_GRPC_PACKAGE = 'CALENDAR_GRPC_PACKAGE';
