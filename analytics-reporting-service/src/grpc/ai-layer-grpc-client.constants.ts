/** Split out from `ai-layer-grpc-client.module.ts` to avoid a circular import - same reasoning as every other service's own `*-grpc-client.constants.ts`. */
export const AI_LAYER_GRPC_PACKAGE = 'AI_LAYER_GRPC_PACKAGE';
