/**
 * Split out from `compliance-grpc-client.module.ts` to avoid the same
 * circular-import failure `AUDIT_GRPC_PACKAGE`'s own doc comment documents.
 */
export const COMPLIANCE_GRPC_PACKAGE = 'COMPLIANCE_GRPC_PACKAGE';
