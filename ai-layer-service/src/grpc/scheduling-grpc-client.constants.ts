/**
 * Split out from `scheduling-grpc-client.module.ts` to avoid the same
 * circular-import failure `AUDIT_GRPC_PACKAGE`'s own doc comment documents.
 */
export const SCHEDULING_GRPC_PACKAGE = 'SCHEDULING_GRPC_PACKAGE';
