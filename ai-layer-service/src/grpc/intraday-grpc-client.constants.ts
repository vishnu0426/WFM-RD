/**
 * Split out from `intraday-grpc-client.module.ts` to avoid the same
 * circular-import failure `AUDIT_GRPC_PACKAGE`'s own doc comment documents.
 */
export const INTRADAY_GRPC_PACKAGE = 'INTRADAY_GRPC_PACKAGE';
