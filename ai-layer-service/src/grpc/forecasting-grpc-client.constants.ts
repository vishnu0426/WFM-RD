/**
 * Split out from `forecasting-grpc-client.module.ts` to avoid the same
 * circular-import failure `AUDIT_GRPC_PACKAGE`'s own doc comment documents.
 */
export const FORECASTING_GRPC_PACKAGE = 'FORECASTING_GRPC_PACKAGE';
