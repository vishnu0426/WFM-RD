/**
 * Split out from `policy-grpc-client.module.ts` specifically to avoid a
 * circular import - same reasoning as
 * `notification-preference-grpc-client.constants.ts`.
 */
export const POLICY_GRPC_PACKAGE = 'POLICY_GRPC_PACKAGE';
