/** Split out from `compliance-grpc-client.module.ts` to avoid a circular import - same reasoning as every other service's own `*-grpc-client.constants.ts` in this platform. */
export const COMPLIANCE_GRPC_PACKAGE = 'COMPLIANCE_GRPC_PACKAGE';
